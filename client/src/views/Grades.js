import React, {useState} from 'react';
import {Container} from 'reactstrap';

const Grades = (props) => {
    return (
        <Container>
            <h1>Grades</h1>
        </Container>
    );
}

export default Grades;