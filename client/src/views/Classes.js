import React, {useState} from 'react';
import {Container} from 'reactstrap';

const Classes = (props) => {
    return (
        <Container>
            <h1>Grades</h1>
        </Container>
    );
}

export default Classes;
